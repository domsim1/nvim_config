require('nightfox').setup({
  options = {
    styles = {
      keywords = 'italic,bold',
      types = 'italic',
      constants = 'bold',
      conditionals = 'italic,bold',
    }
  }
})
vim.cmd('colorscheme terafox')

